/**
 * The UnoSide enum represents the two sides (light and dark) in the Uno card game.
 */
public enum UnoSide {
    /** The light side in Uno. */
    LIGHT,

    /** The dark side in Uno. */
    DARK;

    /**
     * Private constructor for the UnoSide enum.
     */
    private UnoSide() {
    }
}
